# Supabase, shared database. Apply db/schema.sql with the russ_ prefix in phase 1,
# then copy this file to .env.local and fill in the project values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
